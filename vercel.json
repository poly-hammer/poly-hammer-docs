{
    "version": 2,
    "builds": [
    {
      "src": "mkdocs.yml",
      "use": "@vercel/python"
    },
    {
      "src": "site/**/*",
      "use": "@vercel/static"
    }
  ],
    "routes": [
      {
        "src": "/(.*)",
        "dest": "/site/$1"
      }
    ],
    "buildCommand": "pip3 install -r requirements.txt && mkdocs build"
  }